- tcc fails:
  - mingw: 101_cleanup 119_random_stuff 129_scopes
  - darwin: 73_arm64 106_versym 108_constructor 119_random_stuff 120_alias
            124_atomic_counter 128_run_atexit 140_arm64_extasm
- torture fails (common):
  - darwin: 20020411-1 pr58277-1 pr82210 va-arg-22 va-arg-pack-1
  - linux: 20020411-1 pr58277-1 pr58277-2 pr82210 va-arg-22 va-arg-pack-1
- torture fails (arm64-only, 11 tests):
  - va_list/vprintf: fprintf-chk-1 printf-chk-1 va-arg-21 vfprintf-1
    vfprintf-chk-1 vprintf-1 vprintf-chk-1
    (ARM64 va_list struct layout differs from x86_64: 32-byte
    [__stack,__gr_top,__vr_top,__gr_offs,__vr_offs] vs 24-byte va_list[1])
  - string opts: string-opt-18 strlen-7
    (memcpy(ptr,ptr,N)==ptr builtin replacement on ARM64)
  - complex: pr104604 (_Complex unsigned division on ARM64)
  - builtin overflow: pr91450-1 (__builtin_mul_overflow on ARM64)
- run the peephole checks directly in the codegen pass.
  it only needs to checks against the previously emitted line.
- emit asm bytes/.o files directly, no need for GAS, we only need the linker then.
  the linker can be mold or gold. this saves a ton of space.
- add -O2 passes, like -finline and -funroll, but no need for SSA analysis.
- for the CTFE check, set more pure attributes to known library function calls,
  which are pure in the given arguments. observe the purity of calls in CTFE
- optional QBE backend for -O2
- C23 (https://en.cppreference.com/c/compiler_support/23):
  _BitInt, bool_constant, attributes: [[nodiscard]], [[maybe_unused]], [[deprecated]],
  [[fallthrough]], [[noreturn]], [[unsequenced]] and [[reproducible]], u8, ckdint,
  nullptr, static_assert, decimal, #elifde/#elifndef,
  #warning, #embed, 0b constants, __has_c_attribute, __has_include, constexpr objects,
  __VA_OPT__, Storage-class specifiers for compound literals, Relaxed requirements for
  variadic parameter list, auto/__auto_type/typeof, enum > int, digit separators,
  interchange and extended float types, FENV_ACCESS.
